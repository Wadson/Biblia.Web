<div class="nav-menu @(IsCollapsed ? "is-collapsed" : null)">
    <div class="sidebar-top">
        <div class="brand">
            <img class="brand-icon" src="appicon.png" alt="Ícone BíbliaTema" />
            <span class="brand-copy"><strong>BíbliaTema</strong><small>Estudo bíblico</small></span>
        </div>
        <button type="button" class="sidebar-toggle" @onclick="OnToggle"
                aria-label="@(IsCollapsed ? "Expandir menu lateral" : "Contrair menu lateral")"
                aria-expanded="@(!IsCollapsed)"
                title="@(IsCollapsed ? "Expandir menu lateral" : "Contrair menu lateral")">
            <AppIcon Name="menu" Size="lg" />
        </button>
    </div>

    <nav class="nav-list" aria-label="Navegação principal">
        <NavLink href="" Match="NavLinkMatch.All" title="Início" aria-label="Início"><AppIcon Name="home" Size="lg" /><span class="nav-label">Início</span></NavLink>
        <div class="nav-group">BÍBLIA</div>
        <NavLink href="biblia" title="Ler Bíblia" aria-label="Ler Bíblia"><AppIcon Name="book" Size="lg" /><span class="nav-label">Ler Bíblia</span></NavLink>
        <NavLink href="pesquisar-biblia" title="Pesquisar" aria-label="Pesquisar"><AppIcon Name="search" Size="lg" /><span class="nav-label">Pesquisar</span></NavLink>
        <NavLink href="comparacao" title="Comparar versões" aria-label="Comparar versões"><AppIcon Name="compare" Size="lg" /><span class="nav-label">Comparar versões</span></NavLink>
        <NavLink href="versoes" title="Versões bíblicas" aria-label="Versões bíblicas"><AppIcon Name="versions" Size="lg" /><span class="nav-label">Versões bíblicas</span></NavLink>

        <div class="nav-group">TEMAS E VERSÍCULOS</div>
        <NavLink href="temas" Match="NavLinkMatch.All" title="Temas" aria-label="Temas"><AppIcon Name="theme" Size="lg" /><span class="nav-label">Temas</span></NavLink>
        <NavLink href="publicacoes" title="Publicações" aria-label="Publicações"><AppIcon Name="report" Size="lg" /><span class="nav-label">Publicações</span></NavLink>
        <NavLink href="organizacao" title="Igreja / Ministério" aria-label="Igreja / Ministério"><AppIcon Name="settings" Size="lg" /><span class="nav-label">Igreja / Ministério</span></NavLink>
        <NavLink href="temas/vincular-versiculos" title="Vincular versículos" aria-label="Vincular versículos"><AppIcon Name="add" Size="lg" /><span class="nav-label">Vincular versículos</span></NavLink>
        <NavLink href="referencias" title="Referências guardadas" aria-label="Referências guardadas"><AppIcon Name="star" Size="lg" /><span class="nav-label">Referências guardadas</span></NavLink>
        <NavLink href="cards" title="Estúdio de cards" aria-label="Estúdio de cards"><AppIcon Name="image" Size="lg" /><span class="nav-label">Estúdio de cards</span></NavLink>

        <div class="nav-group">SISTEMA</div>
        <NavLink href="pesquisa-global" title="Pesquisa global" aria-label="Pesquisa global"><AppIcon Name="search" Size="lg" /><span class="nav-label">Pesquisa global</span></NavLink>
        <NavLink href="relatorios" title="Relatórios" aria-label="Relatórios"><AppIcon Name="report" Size="lg" /><span class="nav-label">Relatórios</span></NavLink>
        <NavLink href="configuracoes" title="Configurações" aria-label="Configurações"><AppIcon Name="settings" Size="lg" /><span class="nav-label">Configurações</span></NavLink>
    </nav>
</div>

@code {
    [Parameter] public bool IsCollapsed { get; set; }
    [Parameter] public EventCallback OnToggle { get; set; }
}
